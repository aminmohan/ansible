[defaults]
inventory = inventory
#private_key_file = /home/ansible/.ssh/id_rsa
remote_user = ansible
[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = True

